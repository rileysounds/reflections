import UIKit
import SwiftUI

class ShopViewController: UIHostingController<ShopView> { }


struct ShopView: View {
    var body: some View {
        Text("Links to Shop here?")
        Text("Info about events / artists / etc?")
    }
}
